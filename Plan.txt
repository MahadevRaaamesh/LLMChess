To create an app that lets LLm play with each other on a chess board and show the moves on the board.

Chess Engine:
-> use Python chess as the engine , build the validation and all with this

Agents:
->use langchain for this.
-> To create "Agents" that are 
   1. That thinks like an agent , chain of thought.
   2. Looks at the board, writes strategy and updates , removes it from its storage or notes , has a "memory" and makes a move.
   3. If the Agent makes a wrong / illegal move we send that its illegal move and ask it to think again.
-> Agent is choosen first ,like local llm or some api (do this as general as possible and ask the user for key or smthing)

Visuals:
-> UI to show the moves on the board and the agents thinking , each agents thinking on each side like a text thingy
-> Full python project , use a python ui library for this 
-> use the board from the python chess library for the chess board visualization




-> read all the library as needed.

